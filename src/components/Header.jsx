import styles from '../style'
import NavBar from './NavBar'
const Header = () => {
  return (
    <div className={`bg-black text-[#fff] p-[10px]`}>
      <NavBar />
    </div>
  )
}

export default Header